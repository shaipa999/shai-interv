apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simple-webv1.fullname" . }}-test-connection"
  labels:
    {{- include "simple-webv1.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['http://simple-webv1-ingress.com/']
  restartPolicy: Never
